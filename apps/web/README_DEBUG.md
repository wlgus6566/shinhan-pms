# 디버깅 가이드

## 프로젝트 목록이 보이지 않을 때

### 1. 브라우저 콘솔 확인
1. 브라우저에서 F12 또는 Cmd+Option+I (Mac) 키를 눌러 개발자 도구를 엽니다
2. Console 탭을 확인합니다
3. 다음과 같은 로그를 찾아보세요:
   - `🔐 ProjectsPage - Auth 상태:` - 로그인 상태 확인
   - `🔄 프로젝트 목록 조회 시작...` - API 호출 시작
   - `📡 API 호출:` - 실제 API 요청
   - `✅ 프로젝트 목록 조회 성공:` - 성공 시
   - `❌ 프로젝트 목록 조회 실패:` - 실패 시

### 2. 네트워크 탭 확인
1. 개발자 도구에서 Network 탭을 클릭합니다
2. 페이지를 새로고침합니다
3. `/api/projects` 요청을 찾습니다
4. 상태 코드를 확인합니다:
   - 200: 성공
   - 401: 인증 필요 (로그인 필요)
   - 404: API 라우트를 찾을 수 없음
   - 500: 서버 에러

### 3. 로그인 확인
1. 로그아웃 후 다시 로그인해 보세요
2. 테스트 계정:
   - 이메일: `kim@shinhan.com`
   - 비밀번호: `password123`

### 4. LocalStorage 확인
1. 개발자 도구 > Application 탭 > Local Storage
2. 다음 항목이 있는지 확인:
   - `accessToken`: 로그인 토큰
   - `user`: 사용자 정보 (JSON)

### 5. 캐시 지우기
1. Cmd+Shift+R (Mac) 또는 Ctrl+Shift+R (Windows)로 강력 새로고침
2. 또는 개발자 도구에서 Network 탭 > Disable cache 체크

## 문제 해결

### 문제: 로그인은 되었는데 목록이 안 보임
- 콘솔에서 `❌` 에러 메시지를 확인하세요
- Network 탭에서 `/api/projects` 요청의 응답을 확인하세요

### 문제: API 요청 자체가 안 됨
- 콘솔에 `🔄 프로젝트 목록 조회 시작...` 로그가 있는지 확인
- 없다면 컴포넌트가 마운트되지 않은 것입니다

### 문제: 401 Unauthorized
- 로그아웃 후 다시 로그인하세요
- LocalStorage에서 `accessToken`과 `user`를 확인하세요

## 개발 서버 상태 확인

```bash
# 포트 3001이 사용 중인지 확인
lsof -i:3001

# 개발 서버 재시작
cd apps/web
pnpm dev
```

## 빠른 테스트

브라우저 콘솔에서 다음 명령어를 실행:

```javascript
// 1. 로그인 상태 확인
console.log('Token:', localStorage.getItem('accessToken'));
console.log('User:', JSON.parse(localStorage.getItem('user') || '{}'));

// 2. API 직접 호출 테스트
fetch('/api/projects')
  .then(r => r.json())
  .then(data => console.log('Projects:', data))
  .catch(err => console.error('Error:', err));
```
